abstract class CharacterSubCategory {
  //Marker Class
  // This class describes a sub-types, type.
  // Or in other words a sub-types -> Sub type.
  // See the creature -> Construct -> any construct type as an example.
}