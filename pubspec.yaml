name: FrostLibrary
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  path: ^1.6.4
  yaml: ^2.2.1
  built_collection: '>=2.0.0 <5.0.0'
  built_value: ^7.0.2
  build_runner: ^1.0.0
  built_value_generator: ^7.0.9

flutter:
  assets:
    - lib/assets/item_defs/potions.yaml
    - lib/assets/item_defs/weapons.yaml
    - lib/assets/item_defs/spells.yaml

dev_dependencies:
  pedantic: ^1.9.0
#  test: ^1.15.2
  flutter_test:
    sdk: flutter

